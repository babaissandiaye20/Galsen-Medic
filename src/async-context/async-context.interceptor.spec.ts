import { AsyncContextInterceptor } from './async-context.interceptor';

describe('AsyncContextInterceptor', () => {
  it('should be defined', () => {
    expect(new AsyncContextInterceptor()).toBeDefined();
  });
});
